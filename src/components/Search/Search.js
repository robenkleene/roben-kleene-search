import React from "react";

const Search = () => (
  <div id="search">
    <input type="text" />{" "}
    <button>Search</button>
  </div>
);

Search.propTypes = {};

Search.defaultProps = {};

export default Search;
