import React from 'react';
import PropTypes from 'prop-types';
import './Search.css';

const Search = () => (
  <div className="Search">
    <input type="text" />
  </div>
);

Search.propTypes = {};

Search.defaultProps = {};

export default Search;
