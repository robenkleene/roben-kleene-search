import React from "react";
import PropTypes from "prop-types";
import "./Search.css";

const Search = () => (
  <>
    <input type="text" />
    <button>Search</button>
  </>
);

Search.propTypes = {};

Search.defaultProps = {};

export default Search;
