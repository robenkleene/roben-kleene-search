import React from 'react';
import PropTypes from 'prop-types';
import './Engine.css';

const Engine = () => (
  <div className="Engine">
    Engine Component
  </div>
);

Engine.propTypes = {};

Engine.defaultProps = {};

export default Engine;
